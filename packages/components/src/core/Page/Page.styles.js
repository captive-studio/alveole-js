import { makeStyles } from '@alveole/theme';
export const useStyles = makeStyles(() => ({
  gradientBackground: {
    flex: 1,
    width: '100%',
  },
  sidebarContainer: {
    display: 'flex',
    flexDirection: 'row',
    flex: 1,
    backgroundColor: 'transparent',
  },
}));
