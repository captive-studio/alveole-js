import { makeStyles } from '@alveole/theme';

export const useStyles = makeStyles(() => ({
  highlightContainer: {
    width: '100%',
  },
  highlight: {
    overflow: 'scroll',
    borderRadius: 8,
    padding: 16,
    width: '100%',
  },
}));
