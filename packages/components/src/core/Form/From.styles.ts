import { makeStyles } from '@alveole/theme';

export const useStyles = makeStyles(({ color }) => ({
  form: {},
  iosPaddingKeyboard: {
    paddingBottom: 200,
  },
  androidPaddingKeyboard: {
    paddingBottom: 250,
  },
}));
