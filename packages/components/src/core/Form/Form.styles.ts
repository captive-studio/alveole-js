import { makeStyles } from '@alveole/theme';

export const useStyles = makeStyles(() => ({
  form: {},
  iosPaddingKeyboard: {
    paddingBottom: 200,
  },
  androidPaddingKeyboard: {
    paddingBottom: 250,
  },
}));
